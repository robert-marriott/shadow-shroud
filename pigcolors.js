//If the colors library is too slow, make own color methods.



// for(var i = 0; i<reds.length; i++) { reds[i].pwmWrite(0); }
// for(var i = 0; i<grns.length; i++) { grns[i].pwmWrite(255); }
// for(var i = 0; i<blus.length; i++) { blus[i].pwmWrite(0); }
